// Provider adapters are intentionally NOT configured with credentials here.
// Runtime secrets belong in Vercel environment variables, never Git.
// Adding a paid provider does not authorize a paid call.
export function providerRegistry(){
 return [];
}
